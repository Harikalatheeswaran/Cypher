# Cypher
Cypher is a python-based tool to securely encrypt and decrypt files in a folder using AES encryption (Fernet). This program allows users to select a folder via a GUI, encrypt all files recursively while preserving the folder structure, and decrypt them using a password.
